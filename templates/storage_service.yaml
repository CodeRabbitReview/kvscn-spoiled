apiVersion: v1
kind: Service
metadata:
  name: storage-service
spec:
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http-server
  selector:
    app: storage
